<% provide :title, t(".cart") %>
<% provide :breadcrumb, t(".cart") %>
<%= render "commons/breadcrumb" %>

<!-- Shopping Cart Section Begin -->
<section class="shopping-cart spad">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="cart-table">
          <%= render "cart" %>
        </div>
        <div class="row">
          <div class="col-lg-4">
            <div class="cart-buttons">
              <%= link_to  t("continue_shopping"), root_path, class: "primary-btn continue-shop" %>
            </div>
          </div>
          <div class="col-lg-4 offset-lg-4">
            <div class="proceed-checkout">
              <ul>
                <li class="cart-total">Total 
                  <span><%= number_to_currency total_price_cart(@carts) %></span>
                </li>
              </ul>
              <a href="#" class="proceed-btn"><%= t :checkout %></a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<!-- Shopping Cart Section End -->
